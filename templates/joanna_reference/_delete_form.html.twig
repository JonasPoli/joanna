<form method="post" action="{{ path(route_prefix ~ '_delete', {'id': joanna_reference.id}) }}" onsubmit="return confirm('Tem certeza que deseja apagar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ joanna_reference.id) }}">
    <button class="btn text-red-600 hover:text-red-900">Apagar</button>
</form>
